# Tie-Breaking Kernel API

This section documents **deterministic tie-breaking kernels** provided by
eb-optimization. These utilities resolve ambiguity when multiple candidate parameters
yield equivalent objective values.

All content below is generated automatically from **NumPy-style docstrings** in the
source code.

## Tie-Breaking Kernels

::: eb_optimization.search.kernels
