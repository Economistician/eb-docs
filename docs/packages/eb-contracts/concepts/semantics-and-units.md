# Semantics and Units

Semantics define **what a value represents**.
Units define **how it is measured**.

Both are required for interpretability.

---

## Why this matters

Numerically valid values can still be semantically wrong.
EB contracts prevent this class of failure.
