name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MkDocs tooling + plugins
        run: |
          python -m pip install --upgrade pip
          pip install \
            mkdocs-material \
            mkdocstrings[python] \
            pymdown-extensions

      - name: Install Electric Barometer packages (from submodules)
        run: |
          pip install -e external/eb-metrics
          pip install -e external/eb-evaluation
          pip install -e external/eb-adapters
          pip install -e external/eb-features

      - name: Sync package docs into unified tree
        shell: pwsh
        run: |
          ./scripts/sync-packages-docs.ps1

      - name: Deploy to GitHub Pages (gh-pages branch)
        run: |
          mkdocs gh-deploy --clean --force